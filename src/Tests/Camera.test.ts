import { vec3 } from "gl-matrix";
import { describe, expect, test } from "@jest/globals";
import { Camera } from "../Renderer/Camera";

describe("Camera Look At Tests", () => {
	test("Camera position (0, 0, 1) look at (0, 0, 0)", () => {});
});
